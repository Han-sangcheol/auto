# ==========================================
# CleonAI 자동매매 - 32bit Python 전용
# ==========================================
# 키움 API 자동매매 핵심 기능만 포함
# 빌드가 필요한 패키지 제외 (Visual Studio 불필요)
#
# 설치: pip install -r requirements_32bit.txt
#

# ==========================================
# 필수 패키지 (키움 API 자동매매)
# ==========================================

# 키움 API 연동
PyQt5==5.15.9

# 데이터 처리 (기본)
numpy==1.24.3
pandas==1.5.3

# 환경 변수 관리
python-dotenv>=1.0.0

# 로깅
loguru>=0.7.0

# 날짜/시간 처리
python-dateutil>=2.8.2

# 데이터 저장
openpyxl>=3.1.2

# ==========================================
# 선택적 기능 (빌드 불필요)
# ==========================================

# 뉴스 크롤링
beautifulsoup4>=4.12.0
requests>=2.31.0

# Windows 알림
win10toast>=0.9

# 기본 차트 (빌드 불필요)
pyqtgraph>=0.13.3
PyQtWebEngine>=5.15.0

# ==========================================
# 제외된 패키지 (64bit 분석 모듈에서 사용)
# ==========================================
# psutil - Visual C++ 빌드 필요 → 헬스 모니터 비활성화
# yfinance - curl_cffi 빌드 필요 → 외부 데이터 조회 제외
# plotly - 고급 차트 → 64bit 분석 모듈에서 사용
# pandas-ta - 기술적 지표 → indicators.py로 대체

