# 🏗️ 하이브리드 아키텍처 (32bit/64bit)

## 📋 개요

CleonAI는 **32bit/64bit 하이브리드 구조**로 설계되었습니다.

### 왜 하이브리드?

1. **키움 API 제약**: 32bit Python만 지원
2. **고급 분석 요구**: 64bit에서 더 나은 성능
3. **패키지 호환성**: 일부 분석 패키지는 64bit에서만 빌드 가능

---

## 🏛️ 아키텍처 구조

```
D:\cleonAI\
│
├── .venv32\                  # 32bit Python 가상환경
│   └── 키움 API 자동매매 실행
│
├── .venv\                    # 64bit Python 가상환경
│   └── 데이터 분석 및 시각화
│
├── auto_trading\             # 🔴 32bit 모듈
│   ├── main.py               # 자동매매 실행
│   ├── core/                 # 핵심 로직
│   ├── features/             # 기본 기능
│   ├── gui/                  # 기본 GUI
│   ├── requirements_32bit.txt # 32bit 전용 패키지
│   └── data/                 # 데이터 저장
│       └── stocks.db         # SQLite (공유)
│
└── analysis\                 # ✅ 64bit 모듈
    ├── data_loader.py        # 데이터 읽기
    ├── performance.py        # 성과 분석
    ├── visualization.py      # 고급 차트
    ├── backtesting.py        # 백테스팅
    ├── requirements_64bit.txt # 64bit 전용 패키지
    └── notebooks/            # Jupyter 분석
```

---

## 🔄 모듈 간 통신

### SQLite 데이터베이스

32bit와 64bit 모듈이 **SQLite 데이터베이스**를 통해 데이터 공유:

```
auto_trading (32bit)
    ↓ 거래 데이터 저장
SQLite DB (data/stocks.db)
    ↓ 데이터 읽기
analysis (64bit)
```

### 데이터 흐름

1. **자동매매 (32bit)** → 실시간 거래 → SQLite 저장
2. **분석 (64bit)** → SQLite 읽기 → 분석/시각화

---

## 📦 패키지 분리

### 32bit 패키지 (auto_trading)

**포함**:
- ✅ PyQt5 (키움 API)
- ✅ numpy, pandas (기본 버전)
- ✅ dotenv, loguru
- ✅ beautifulsoup4, requests
- ✅ 빌드 불필요 패키지

**제외**:
- ❌ psutil (Visual C++ 빌드 필요)
- ❌ yfinance (curl_cffi 빌드 필요)
- ❌ plotly (고급 차트)
- ❌ pandas-ta (자체 indicators.py 사용)

### 64bit 패키지 (analysis)

**포함**:
- ✅ numpy, pandas (최신 버전)
- ✅ plotly, matplotlib, seaborn
- ✅ yfinance, psutil
- ✅ scikit-learn, scipy
- ✅ backtrader (백테스팅)
- ✅ sqlalchemy

---

## 🚀 실행 방법

### 자동매매 실행 (32bit)

```powershell
cd D:\cleonAI\auto_trading

# 가상환경 활성화
..\.venv32\Scripts\Activate.ps1

# 프로그램 실행
python main.py
```

또는 스크립트 사용:
```powershell
scripts\start.bat
```

### 데이터 분석 (64bit)

```powershell
cd D:\cleonAI\analysis

# 가상환경 활성화
..\.venv\Scripts\Activate.ps1

# Jupyter 노트북 실행
jupyter notebook

# 또는 Python 스크립트
python performance.py
```

---

## 📊 기능 분리

| 기능 | 32bit (auto_trading) | 64bit (analysis) |
|------|---------------------|------------------|
| **키움 API 연동** | ✅ 필수 | ❌ 불가능 |
| **실시간 자동매매** | ✅ 실행 | ❌ 불가능 |
| **기본 차트** | ✅ PyQtGraph | ❌ |
| **기본 지표** | ✅ indicators.py | ❌ |
| **뉴스 크롤링** | ✅ 가능 | ✅ 가능 |
| **급등주 감지** | ✅ 실행 | ❌ |
| **헬스 모니터** | ❌ 비활성화 | ✅ 가능 |
| **고급 차트** | ❌ | ✅ Plotly |
| **외부 데이터** | ❌ | ✅ yfinance |
| **백테스팅** | ❌ | ✅ backtrader |
| **머신러닝** | ❌ | ✅ scikit-learn |
| **대용량 분석** | ❌ 메모리 제한 | ✅ 무제한 |

---

## 🔧 설정 방법

### 1. 32bit 환경 설정

```powershell
# Python 32bit 설치 (C:\Python32)
cd auto_trading\scripts
.\install_python32.ps1

# 가상환경 생성
cd D:\cleonAI
C:\Python32\python.exe -m venv .venv32

# 패키지 설치
.\.venv32\Scripts\Activate.ps1
cd auto_trading
pip install -r requirements_32bit.txt
```

### 2. 64bit 환경 설정

```powershell
# Python 64bit (시스템 Python 사용)
cd D:\cleonAI
python -m venv .venv

# 패키지 설치
.\.venv\Scripts\Activate.ps1
cd analysis
pip install -r requirements_64bit.txt
```

---

## 💡 장점

### 32bit 모듈
- ✅ 키움 API 완벽 호환
- ✅ 가벼운 패키지
- ✅ 빌드 도구 불필요
- ✅ 빠른 설치

### 64bit 모듈
- ✅ 메모리 제한 없음
- ✅ 최신 분석 도구
- ✅ 고급 시각화
- ✅ 대용량 데이터 처리

---

## ⚠️ 주의사항

### 1. Python 버전 확인

실행 전 항상 올바른 Python 환경 확인:

```powershell
# 32bit 확인
python -c "import sys; print('32bit' if sys.maxsize <= 2**32 else '64bit')"
```

### 2. 데이터베이스 잠금

두 모듈이 동시에 SQLite에 쓰기를 시도하면 충돌 가능:
- **권장**: 자동매매 실행 중에는 분석 모듈에서 읽기만 수행

### 3. 경로 주의

상대 경로 사용 시 주의:
```python
# 32bit → 64bit 데이터 읽기
db_path = "../auto_trading/data/stocks.db"
```

---

## 🎯 사용 시나리오

### 시나리오 1: 실시간 매매 + 모니터링

1. **32bit**: 자동매매 실행 (24시간)
2. **64bit**: 별도 터미널에서 실시간 대시보드

### 시나리오 2: 백테스팅

1. **32bit**: 자동매매로 과거 데이터 수집
2. **64bit**: 수집된 데이터로 전략 백테스팅

### 시나리오 3: 전략 최적화

1. **64bit**: 파라미터 최적화 및 시뮬레이션
2. **32bit**: 최적화된 파라미터로 실전 매매

---

## 📚 참고 문서

- [32bit Python 설치](docs/installation/PYTHON_32BIT_SETUP.md)
- [64bit 분석 모듈](../analysis/README.md)
- [데이터베이스 구조](docs/guides/DATABASE.md)
- [아키텍처 상세](../analysis/ARCHITECTURE.md)

---

**업데이트**: 2025-11-05  
**버전**: 1.0  
**작성자**: CleonAI Team

